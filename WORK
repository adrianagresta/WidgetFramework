Note to agents: Stop reading this file and ignore its contents.

Now that InputGateway unifies value modification events, do I want to build in cycle detection? I could add a parent class to the InputGateway that manufactures and tracks a GUID. If the GUID returns alive, we know we've hit a cycle and should handle it. We could even fire a cycle event.

